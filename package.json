{
  "name": "inhouse-ops",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist && wrangler deploy --dry-run -c wrangler.hooks.toml --outdir dist-hooks",
    "check": "tsc -p tsconfig.json",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "deploy": "wrangler deploy",
    "deploy:hooks": "wrangler deploy -c wrangler.hooks.toml",
    "db:init": "wrangler d1 execute inhouse-ops --file=./schema.sql --remote",
    "prove:gmail": "node prove/gmail.mjs",
    "prove:quo": "node prove/quo.mjs",
    "prove:triage": "node prove/triage.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.131.1"
  }
}
