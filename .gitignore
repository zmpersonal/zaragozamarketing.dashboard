node_modules/
.dev.vars
.env
.wrangler/
.DS_Store
dist/
dist-hooks/

# Credentials: service-account keys live outside the repo. Belt and braces.
secrets/
*.pem
*service-account*.json
inhouse-ops-*.json
# Seed files with real addresses stay outside git; only *.example.sql is tracked.
seeds/*
!seeds/*.example.sql
